ListItems returns a copy of all items in the store, sorted alphabetically by Name. The original slice is not exposed (defensive copy). The sort is case-sensitive (uppercase before lowercase). See <ref spec="report.summary">report summary</ref> which uses this output.