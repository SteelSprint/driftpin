Reset acquires an exclusive state lock for the entire Load→modify→Save window, loads State, scans the filesystem, reconciles specs and markers, then resolves a single drifted edge. The (from, to) arguments name the edge endpoints; CLI dispatches based on dots in the IDs (marker-spec for link-style, spec-spec for ref-style). For spec-spec resets, applyRefEdgeReset first reconciles the baseline edge against scan (add if new, drop if removed), and if the To endpoint is missing on disk the action errors with ErrBrokenEdgeNotResettable. The core stamps an EdgeResolution and collapses; the orchestrator then saves the resulting State (including Edges and Resolutions). For removed spec-spec edges, pruneResolutionsTouchingPair drops any dangling resolutions. See principles.friction.