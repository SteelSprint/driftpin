ResetRef acquires an exclusive state lock for the entire Load→modify→Save window, then resolves a ref-edge todo by dispatching on the current drift state: (a) TodoRefAdded (scan has the edge, baseline does not) → add the edge to baseline Refs; (b) TodoRefRemoved (baseline has the edge, scan does not) → drop the edge from baseline Refs AND prune any RefResolutions referencing the edge in either direction (else Validate would reject the next Load); (c) TodoRefChain (source spec hash-drifted) → stamp a RefResolution at the current source hash, idempotent; (d) TodoBrokenRef (target spec missing) → return ErrBrokenRefNotResettable, requiring the user to fix the spec text or restore the missing spec. Either direction of a declared ref edge is a valid (reviewed, source) pair. See principles.friction.