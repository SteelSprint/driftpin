ResetOrphan acquires an exclusive state lock for the entire Load→modify→Save window, then takes a single ID. If the ID contains a dot, it is treated as a spec ID; otherwise as a marker ID. If the entry is stale (not on disk, no edges), it is removed from state.xml along with any resolutions touching it. If still on disk, errors. If it has edges, errors with guidance to resolve them first. See principles.friction.