Todo loads State, scans the filesystem, reconciles specs and markers, builds a CoreAlgorithmContext with TodoAction (propagating baseline Refs and RefResolutions), runs the core algorithm, and returns the EvaluatedState without saving. The scan's discovered Refs are passed via the Scan struct so the core can detect ref-edge add/remove/broken and propagate rhizomatic closure drift. See principles.friction — review without side effects.