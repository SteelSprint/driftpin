Every command flows through the same dispatch pipeline. The pre-dispatch uniform checks (help_flag, unknown_flag_rejection, global_flag_stripping) and the post-dispatch uniform handling (presenter.Render, exit-code propagation) apply identically to all commands including version. No command bypasses the pipeline. See <ref spec="output.dispatch_pipeline">the dispatch pipeline</ref>.