Link acquires an exclusive state lock (StateStore.Lock) for the entire Load→modify→Save window, loads State, scans the filesystem, reconciles, validates that the marker and spec exist, rejects duplicate links, saves the State with the new link appended (preserving Refs from the current scan and existing RefResolutions), and writes baseline files for the spec and marker using their current scanned content. The lock prevents concurrent invocations from silently overwriting each other's links.