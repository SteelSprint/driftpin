# Driftpin

Driftpin helps LLMs keep specs and code in sync. 

## Quickstart

1. Run `Install command TBD` 
2. Run `drift init` in your project
3. Prompt your LLM: `Save "drift skill" in the local project.`

## Anatomy


