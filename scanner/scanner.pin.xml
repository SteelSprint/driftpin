<module name="scanner">
<spec id="spec_discovery">FileScanner follows imports starting from main.pin.xml, parses each .pin.xml as XML, extracts spec elements with their id attribute, SHA1-hashes the trimmed inner content, and returns Spec records with module-qualified ID, Hash, Filepath, and LineNumber.</spec>
<spec id="marker_discovery">FileScanner walks the project directory for code files by extension, finds lines matching the D! id=shortcode pattern, SHA1-hashes the next 10 lines from the marker line, and returns Marker records with ID, Hash, Filepath, and LineNumber.</spec>
<spec id="hash_sha1">All hashing uses SHA1 hex-encoded via crypto/sha1 and fmt percent-x formatting. The hash is deterministic across runs for identical content.</spec>
<spec id="duplicate_spec">Duplicate spec IDs within the same module cause an error with the duplicate ID quoted.</spec>
<spec id="duplicate_marker">Duplicate marker shortcodes across files cause an error with the duplicate shortcode quoted.</spec>
<spec id="missing_id">A spec element without an id attribute causes an error.</spec>
<spec id="code_extensions">Only files with recognized code extensions are scanned for markers: .go, .py, .js, .ts, .jsx, .tsx, .java, .c, .cpp, .h, .hpp, .rs, .rb, .php, .swift, .kt, .cs, .scala, .sh, .bash, .lua, .dart, .vue, .svelte.</spec>
<spec id="specs_wrapper_rejection">If spec elements are nested inside a specs wrapper element under main or module, the scanner returns an error explaining that specs must be direct children of the root element, not inside a specs wrapper.</spec>
<spec id="spec_id_format">The id attribute in a spec element must not contain a dot. The scanner constructs the fully-qualified spec ID as moduleName + "." + localId. This ensures every qualified spec ID has exactly one dot, separating module from local ID.</spec>
<spec id="marker_id_format">Marker shortcodes must not contain a dot. The scanner rejects any marker ID containing a dot with an error. Dots are reserved for spec ID qualification, enabling unambiguous disambiguation in CLI commands.</spec>
</module>
